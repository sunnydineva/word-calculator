package org.example;

public class Exc2 {
    public static void main(String[] args) {

        //
        // have the function MathChallenge (num1, num2) take both parameters being passed and return the Greatest
        // Common Factor. That is, return the greatest number that every goes into both number with no remainder.
        // For example: 12 and 16 both are dividsible by 1, 2, and 4 so, the output should be 4.
        // The range for both parameters will be from1 to 10^3. Example: Input 7 & num2 = 3, Output : 1
    }
}
